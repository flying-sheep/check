name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Integration test 1
        uses: ./
        with:
          condition: true
      - name: Integration test 2
        uses: ./
        with:
          condition: "true"
      - name: Integration test 2
        uses: ./
        with:
          condition: ${{ 'a' == 'a' }}
